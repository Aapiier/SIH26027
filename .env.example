# RailSync AI - Production Backend Environment Configuration

# Server Port (assigned automatically by cloud hosts e.g. Render / Railway / Fly.io)
PORT=8000

# Allowed CORS Origins (comma-separated, leave * for public prototype)
ALLOWED_ORIGINS=*
