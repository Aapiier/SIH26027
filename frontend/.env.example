# RailSync AI - Frontend Environment Configuration

# Set to your deployed FastAPI backend URL on Render/Railway/Fly.io
# (e.g. https://railsync-api.onrender.com)
# Leave empty during local development to use Vite's built-in proxy (http://127.0.0.1:8000)
VITE_API_URL=
